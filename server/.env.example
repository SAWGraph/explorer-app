# Backend
DATABASE_URL=postgres://user:password@localhost:5432/sawgraph
# Origin allowed by CORS (frontend URL). In dev: http://localhost:5173
FRONTEND_ORIGIN=http://localhost:5173
# Optional: set PGSSL=disable for local Postgres without SSL
PGSSL=disable
PORT=3001
